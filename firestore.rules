rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Cheer App: Allow read & write for all collections in development/demo
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
